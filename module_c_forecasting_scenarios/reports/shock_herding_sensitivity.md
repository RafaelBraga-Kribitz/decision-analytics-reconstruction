# Shock & herding parameter sensitivity (IMP-C04 / audit C5)

Generated by `module_c_forecasting_scenarios/reports/generate_shock_herding_sensitivity.py` against the canonical fixture (`tests/fixtures/polls_raw_fixture.csv`, 8 tracking polls). Deterministic — no RNG.

**Config hash (shock_params.yaml + herding_groups.yaml):** `25eb628aea94f158b38b8c18d4c4021c2ec44d1ec2cac5519560f71dfa3d06c0`

Every parameter perturbed here carries a `hypothesis` provenance row in `config/shock_params.yaml` / `config/herding_groups.yaml` — none is fit against a historical polling-error dataset. This artifact is the pre-registered sensitivity envelope that accompanies that disclosure.

## Bucket-assignment reassignment fraction (flagged `assignment-critical` when either +/-25% perturbation reassigns > 20% of polls)

| Parameter | -50% | -25% | +25% | +50% | assignment-critical? |
|---|---|---|---|---|---|
| `m_star_extreme_pp` | 37.5% | 12.5% | 0.0% | 0.0% | no |
| `phi_opaque_threshold` | 0.0% | 0.0% | 0.0% | 0.0% | no |
| `rho_herd_threshold` | 0.0% | 0.0% | 0.0% | 0.0% | no |
| `herding_covariance_march_elevated` | 0.0% | 0.0% | 0.0% | 0.0% | no |
| `herding_covariance_march_baseline` | 0.0% | 0.0% | 0.0% | 0.0% | no |
| `herding_covariance_april` | 0.0% | 0.0% | 0.0% | 0.0% | no |
| `herding_covariance_outside` | 0.0% | 0.0% | 0.0% | 0.0% | no |

No parameter is `assignment-critical` on the canonical fixture at the current values — bucket assignment is stable under +/-25% perturbation of every threshold.

## Continuous shock-score sensitivity (lambda1/2/3 — do not enter bucket assignment)

`scenario_bucket_for_margin` never reads `lambda1`/`lambda2`/`lambda3` — they only scale the continuous `shock_score_s` (used for within-bucket Monte Carlo sampling weights, IMP-C08). Their bucket-reassignment fraction is therefore *definitionally* 0.0 at every perturbation level; the mean relative change in `shock_score_s` is reported instead as their genuine sensitivity surface.

| Parameter | mean |delta score| / score at -50% | -25% | +25% | +50% |
|---|---|---|---|---|
| `lambda1` | 46.3% | 23.2% | 23.2% | 46.3% |
| `lambda2` | 2.7% | 1.4% | 1.4% | 2.7% |
| `lambda3` | 1.0% | 0.5% | 0.5% | 1.0% |
