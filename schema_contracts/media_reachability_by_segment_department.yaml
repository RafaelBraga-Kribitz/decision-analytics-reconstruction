schema_name: media_reachability_by_segment_department
schema_version: "1.1.0"
description: >
  Department-grained companion to media_reachability_by_segment.
  One row per (segment_label, department) pair.  Required so Module B can
  build channel caps and persuasion-efficiency multipliers at the geographic
  granularity its LP/MILP actually consumes.
  Produced by: module_a (aggregation step).
  Consumed by: Module B (reach caps per department per segment).
  v1.1.0 (IMP-B02 / issue #58): each measured mean carries a *_se standard
  error (sample std / sqrt(n) of the cell; NaN for n<=1 cells) so uncertainty
  crosses the A->B boundary instead of point estimates being treated as
  certain. UNCERTAINTY PROVENANCE: participation_propensity_se is the
  DESCRIPTIVE dispersion of the shipped propensity scores — which includes
  the cosmetic within-department spread documented in the propensity model
  card (F-079) — quantifying cell-mean stability under entity resampling,
  not model/posterior uncertainty.

unique_key:
  - segment_label
  - department

row_count:
  # k=6 segments * 18 departments; empty cells (no entity assigned to a
  # segment in a department) are emitted with segment_size=0 so the artifact
  # is dense.
  exact: 108

fields:
  segment_label:
    type: string
    nullable: false
    allowed_values:
      - rural_committed
      - urban_high_volatility
      - youth_volatile
      - structurally_dependent_bloc
      - rural_low_propensity
      - committed_opposition
  department:
    type: string
    nullable: false
    allowed_values:
      - Asuncion
      - Concepcion
      - San Pedro
      - Cordillera
      - Guaira
      - Caaguazu
      - Caazapa
      - Itapua
      - Misiones
      - Paraguari
      - Alto Parana
      - Central
      - Neembucu
      - Amambay
      - Canindeyu
      - Presidente Hayes
      - Boqueron
      - Alto Paraguay
  segment_size:
    type: int64
    nullable: false
    min: 0
  segment_size_pct_of_department:
    type: float32
    nullable: false
    min: 0.0
    max: 1.0
  segment_size_pct_of_segment:
    type: float32
    nullable: false
    min: 0.0
    max: 1.0
  mean_participation_propensity:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  participation_propensity_se:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
    description: Standard error of the cell mean (descriptive score dispersion — see contract description).
  pct_internet_access:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  pct_internet_access_se:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  mean_tv_penetration:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  mean_tv_penetration_se:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  mean_radio_penetration:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  mean_radio_penetration_se:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  mean_whatsapp_penetration:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  mean_whatsapp_penetration_se:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  pct_rural:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  pct_jopara:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  pct_structural_dependency:
    type: float32
    nullable: true
    min: 0.0
    max: 1.0
  region:
    type: string
    nullable: false
    allowed_values: [ORIENTAL, CHACO]
    description: Geographic regime tag; CHACO = {Presidente Hayes, Boqueron, Alto Paraguay}.
  primary_reach_channel:
    type: string
    nullable: true
    allowed_values: [tv, radio, whatsapp, direct]
    description: Channel with highest mean penetration in this (segment, department) cell.

quality_gates:
  empty_cells_allowed: true
  segment_size_zero_implies_null_means: true
