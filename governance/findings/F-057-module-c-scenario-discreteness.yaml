id: F-057
title: "Module C C5/C6 misrepresented discrete, exchangeable Monte-Carlo draws"
category: misleading_visualization
kind: recurrence_invariant
status: closed
opened_at: 2026-06-13
closed_at: 2026-06-13
recurrence_count: 0
evidence: |
  governance/ISSUE_TRIAGE_MASTER.yaml AUD-C5 + AUD-C6 (severity reject, shared
  root). reports/eda/generate_eda.py chart_c5 drew a percentile "fan" of
  shock_scale over a 250-draw-chunk index — a meaningless x-axis, since
  Monte-Carlo draws are exchangeable and carry no ordering. chart_c6 fit a
  Gaussian KDE over shock_scale, which clusters at a few discrete point masses
  (near-deterministic scenario design), fabricating smooth continuous density
  that is not in the data.
verification_script: scripts/check_module_c_scenario_discreteness.py
notes: |
  Fix (reports/eda/generate_eda.py + C5/C6 narrative):
  - C5 is now a per-scenario shock-scale distribution (box + jittered draws,
    seed=42) with an on-chart note that draws are exchangeable so a draw-index
    fan would be meaningless; near-deterministic scenarios collapse to a point.
  - C6 drops the KDE entirely: plain histogram + dotted markers at each discrete
    point mass, with a note that KDE would fabricate continuous density.
  Shared theme with F-053 precedent (one finding can resolve a pair of triage
  rows with the same root). Static gate; canonical PNGs regenerate via
  make pipeline-full (F-050). Part of governance/chart_audit_completion_sprint.md.
